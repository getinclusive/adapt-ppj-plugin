version: v1.0
name: Python
agent:
  machine:
    type: e1-standard-2
    os_image: ubuntu1804
blocks:
  - name: 'Block #1'
    dependencies: []
    task:
      jobs:
        - name: 'Job #1'
          commands:
            - aws --version
            - 'curl -LO https://storage.googleapis.com/kubernetes-release/release/v1.18.0/bin/linux/amd64/kubectl'
            - chmod +x ./kubectl \
            - '    && mv ./kubectl /usr/local/bin/kubectl \'
            - '    && kubectl version --clientchmod +x ./kubectl \'
            - '    && mv ./kubectl /usr/local/bin/kubectl \'
            - '    && kubectl version --client'
